# Node / Vite
node_modules/
dist/
.vite/
*.log
npm-debug.log*

# Tauri / Rust
src-tauri/target/
src-tauri/Cargo.lock
src-tauri/gen/
src-tauri/WixTools/

# Sidecar binaries — keep the dev ones, but ignore platform-specifics we
# fetch on demand via scripts/download-wt.sh. The Linux gnu sidecar is
# the only one committed, so other platforms stay out of git.
src-tauri/binaries/wt-*-apple-darwin
src-tauri/binaries/wt-*-apple-darwin.exe
src-tauri/binaries/wt-*-pc-windows-msvc.exe
src-tauri/binaries/wt-*-unknown-linux-musl
src-tauri/binaries/wt-*-unknown-linux-gnu.exe

# Editor / OS
.DS_Store
.idea/
.vscode/
*.swp
*.swo

# Local env
.env
.env.local
