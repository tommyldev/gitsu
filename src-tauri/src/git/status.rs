// Placeholder — real implementation lands in M3 (porcelain v2 status).
// For v1, `wt list --format=json` already gives us everything the
// dashboard needs. See `src-tauri/src/git/mod.rs` for the module roadmap.
