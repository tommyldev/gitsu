// Placeholder for AI-agent session detection (M1.1). Cross-platform
// process inspection — `ps` + `/proc` on Linux, `lsof` on macOS,
// `Get-Process` on Windows — to find claude/codex/opencode processes
// bound to a worktree. See `docs/ARCHITECTURE.md` for the design.
